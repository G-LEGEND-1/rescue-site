# render.yaml
services:
  - type: web
    name: rescue-site
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000  # Render provides PORT automatically